package LinkedIn;
import java.util.*;
/**
 Given a set of points in cartesian plane, find the line that passes through the maximum number of points
 */
public class LineWithMAXPoints {
//    from collections import defaultdict
//    def lineWithMaxNumberOfPoints(points):
//            if len(points) is 0:
//            return None
//    if len(points) is 1:
//            return (0, points[0][1])
//
//    lines = defaultdict(lambda: 1)
//    for i in range(0, len(points) - 1):
//            for j in range(i + 1, len(points)):
//    line = lineThroughTwoPoints(points[i], points[j])
//    lines[line] += 1
//
//            return max(lines, key=lines.get)
//
//    def lineThroughTwoPoints(p, q):
//            if p[0] == q[0]:
//            return (None, p[0] - q[0])
//    slope = (1.0 * p[1] - q[1]) / (p[0] - q[0])
//    interceptY = p[1] - slope * p[0]
//            return (slope, interceptY)


}
